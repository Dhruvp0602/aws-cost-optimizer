{
  "name": "aws-cost-optimizer-root",
  "version": "1.0.0",
  "description": "AWS Cost Optimizer Root - Monorepo Management",
  "private": true,
  "scripts": {
    "start": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix frontend"
  }
}
